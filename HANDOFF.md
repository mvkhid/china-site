# Проект: сайт «Выкуп товаров и оплата в Китай»

Документ-передача для Claude Code. Собрать production-сайт из готового статичного макета.

---

## 0. КРИТИЧНО — прочитать первым

**Гибридная архитектура (решение от 2026-09-15):**
- **Сайт** (Next.js 15, весь UI/страницы) — хостится на **Vercel (Hobby-тариф)**. Изначально был принципиальный запрет на Vercel из-за ToS Hobby-тарифа (коммерческое использование на бесплатном плане запрещено формально), но владелец сознательно принял этот риск — уже ведёт второй проект на Vercel, конкуренты тоже коммерциализируют на Hobby без проблем. Платный тариф брать не планирует.
- **Пайплайн автокурса** (парсинг Telegram-канала) — остаётся на **Cloudflare**: отдельный Worker с Cron Trigger (каждые 15 мин, free-план поддерживает от 1 минуты) пишет в Cloudflare KV и отдаёт `GET /rate` — публичный JSON-эндпоинт. Причина: у Vercel Hobby cron только раз в сутки, это не годится для курса; Cloudflare Workers/KV/Cron Trigger бесплатны и без этого ограничения.
- Next.js-сайт на Vercel запрашивает курс у Cloudflare Worker по HTTP при рендере/на клиенте, с фолбэком на последний известный корректный курс при сбое сети/парсинга.

Раздел 3 и 6 ниже описывают эту связку подробнее.

**Worker автокурса уже задеплоен и работает:** `https://reseller-rate-worker.mgmdgdjiev1.workers.dev` — `GET /rate` отдаёт текущий курс, cron тикает каждые 15 мин, парсер проверен на реальном посте от 15.09 (14.15/14.00/13.95₽). Код в `worker-rate/` в этом репозитории. `ALLOWED_ORIGIN` пока `*` — обновить на домен сайта после деплоя на Vercel.

**Источник истины по дизайну** — файл `preview-opt.html` (загружается вместе с этим документом). Весь CSS, вся разметка, все анимации, тексты и вшитые картинки — там. Ничего не придумывать заново, переносить как есть.

---

## 1. Владелец и реквизиты

- **ИП Магомедгаджиев Аюб Магомедгаджиевич**
- ОГРНИП: `326050000130671`
- ИНН: `050029997326`
- Адрес регистрации: Республика Дагестан, г. Махачкала
- Telegram (личка, все CTA ведут сюда): `https://t.me/mvkhid`
- Канал с курсом (источник автокурса): `https://t.me/reseller_rmb`
- Канал отзывов: `https://t.me/reseller_testimonials`
- E-mail: `mvkhid.info@gmail.com`

**Стиль общения с владельцем:** на «ты», по-русски, лаконично, без длинных преамбул. Предпочитает готовые файлы целиком, а не патчи с инструкциями «вставь сюда». Работает короткими сессиями ~30 мин, решения принимает быстро когда даны чёткие варианты.

---

## 2. Что за сайт

Лендинг услуги выкупа товаров и оплаты поставщиков в Китае. Три страницы, переключаются без перезагрузки.

**Позиционирование важно:** главная — про **опт/выкуп/закупки**. Обменная часть (калькулятор, курс) вынесена на вторую страницу сознательно, по юридическим соображениям. Не выносить курс/обмен на главную.

### Структура

| Страница | Что внутри |
|---|---|
| **Главная** (`page-home`) | Hero (заголовок + 3 карточки услуг справа) → «Где закупаю» (6 площадок) → мини-статы (4 года / от 1¥ / вся РФ) → «От заявки до доставки товара» (4 шага в кружках) → «Обо мне» (фото + текст + 3 принципа) → «Почему мне доверяют» (4 карточки) |
| **Расчёты и курс** (`page-how`) | Калькулятор с тарифными ступенями → 4 шага процесса → чеки операций (marquee) → расписание → 3 инфо-карточки → FAQ (аккордеон) |
| **Контакты** (`page-contacts`) | Telegram + режим работы и география |

Плюс глобальные: шапка, футер, нижний навбар (`.demo-switch`), cookie-баннер, модалка политики, модалка оферты, lightbox для чеков.

---

## 3. Технический стек (гибрид Vercel + Cloudflare)

**Сайт:**
- **Next.js 15** (App Router)
- Деплой — **Vercel** (Hobby-тариф), стандартный git-автодеплой, без адаптеров
- **Framer Motion** — анимации (в макете они на чистом CSS+IntersectionObserver, при переносе можно оставить как есть или перевести на Framer Motion)
- **Шрифт Jost** (Google Fonts, веса 300/400/500/600/700, subset cyrillic+latin)

**Пайплайн автокурса (отдельный сервис на Cloudflare):**
- **Cloudflare Worker** — маленький сервис, не связан с хостингом сайта
- **Cloudflare KV** (`RATE_KV`) — хранит распарсенный курс
- **Cloudflare Cron Trigger** — каждые 15 минут парсит канал, обновляет `RATE_KV`
- Worker отдаёт `GET /rate` — публичный JSON-эндпоинт, его читает Next.js-сайт на Vercel

`NEXT_INC_CACHE_KV` из старого плана (инкрементальный кэш Next.js на Workers) больше не нужен — это был артефакт хостинга на Cloudflare через OpenNext, при хостинге на Vercel кэш ISR встроенный.

**Репозиторий:** `mvkhid/-1` на GitHub. Проверить что жив и подключить автодеплой на Vercel; если репозитория нет — создать новый.

**Прошлый live-адрес:** `aiub-site.mgmdgdjiev1.workers.dev` (там старая версия на Cloudflare, сильно отставшая — после переезда на Vercel этот адрес останется только за Worker'ом курса, либо будет заменён на поддомен воркера).

### Как устроен перенос дизайна в `site/` (прагматичное решение под дедлайн)

Вся разметка `preview-opt.html` использует инлайновые `onclick="..."` и глобальные JS-функции (`showPage()`, `openPrivacy()` и т.д.) — это типичный vanilla-JS паттерн, который не транслируется напрямую в JSX (`onclick` как строка — не то же самое, что React `onClick={fn}`). Чтобы перенести «как есть» без риска внести опечатки при ручной конвертации 1675 строк разметки в JSX под сжатые сроки, сделано так:

- `site/lib/legacy-markup.html` — вся разметка (header → footer → модалки → lightbox) как есть, один raw HTML-блок
- `site/lib/legacy-script.js` — оба `<script>` блока из макета, с точечными правками:
  - `tiers` теперь `let` и подтягивается из `GET /rate` (см. `loadLiveRate()`), с хардкодом как фолбэком
  - инициализация переведена с `DOMContentLoaded` на `onDomReady()` — в Next.js скрипт грузится через `<Script strategy="afterInteractive">`, то есть **после** `DOMContentLoaded`, и обычный listener на это событие никогда не сработает
- `site/app/page.js` — server component, рендерит markup через `dangerouslySetInnerHTML` и скрипт через `next/script`
- `site/app/globals.css` — CSS из макета, перенесён без изменений
- Картинки (7 чеков + фото) вынесены из base64 в `site/public/` скриптом `extract.mjs` (в корне репозитория, разовый — можно удалить)

Это не «правильный» React (компонентов нет), но работает полностью корректно и соответствует требованию HANDOFF «ничего не придумывать заново, переносить как есть». Разбить на компоненты можно позже, не блокирует запуск.

---

## 4. Дизайн-система (из макета)

```css
--bg-deep:#0a1f2b;  --bg-base:#0e2837;  --bg-card:#143247;  --bg-card-2:#173b50;
--line:rgba(255,255,255,.07);  --line-strong:rgba(255,255,255,.13);
--text-primary:#f4f7f8;  --text-secondary:rgba(244,247,248,.70);
--text-muted:rgba(244,247,248,.45);  --text-faint:rgba(244,247,248,.26);
--brand:#4ec9d6;  --brand-soft:rgba(78,201,214,.10);  --brand-glow:rgba(78,201,214,.30);
--font:"Jost", sans-serif;
--radius:14px;  --radius-lg:20px;
--shadow:0 1px 0 rgba(255,255,255,.04) inset, 0 14px 40px -20px rgba(0,0,0,.7);
```

- Контейнер: `max-width:1280px`, паддинги `0 40px` (мобайл `0 20px`)
- Секции полноширинные с чередующимися фонами: `bg-deep` / `bg-lift` / `bg-tint`
- Отступы между секциями 80px (мобайл 50px)
- Брейкпоинты: `1100px`, `860px` (основной), `480px`, `400px`

### Анимации (перенести все)
- **Reveal при скролле** — единый fade-up 0.7s ease-out, разные типы (`reveal-up`, `reveal-left`, `reveal-right`, `reveal-scale`), через IntersectionObserver, threshold 0.12
- **CountUp** на цифрах мини-статов
- **Blob-эффект** (свечение под курсором) на `.btn-primary` и `.tier` — через `mousemove` + rAF-throttle. **Только desktop, на touch намеренно отключён**
- **Marquee** чеков — 45 сек цикл, пауза при наведении, пауза при выходе из viewport (IntersectionObserver), маска-градиент по краям `6%/94%`
- **FAQ-аккордеон** — плавное раскрытие через `grid-template-rows: 0fr → 1fr`, 0.32s ease; при открытии нового предыдущий закрывается; иконка `+` поворачивается на 45° в `×`
- **Декоративные символы** ₽/¥ — разбросаны по секциям, прозрачность 4-5%
- GPU-ускорение: `transform:translateZ(0)` на marquee и шапке
- Поддержка `prefers-reduced-transparency`

---

## 5. Калькулятор — логика (перенести точь-в-точь)

```js
const tiers = [
  {threshold:1,     rate:11.75},
  {threshold:1000,  rate:11.60},
  {threshold:30000, rate:11.55}
];
```

**Важно:** направление только одно — **рубли → юани**. Кнопка свапа удалена намеренно (обратный обмен идёт по другому курсу, чтобы не вводить в заблуждение). Вместо неё статичная стрелка `→` (на мобайле поворачивается на 90°).

Логика:
1. Пользователь вводит сумму в рублях
2. Пересчитываем в юани, по полученной сумме выбираем тарифную ступень (`pickTier`)
3. Результат = рубли / курс ступени
4. Подсвечиваем активную ступень классом `.tier-active`
5. В `#active-info` пишем «Применён курс X ₽ за 1¥ · ступень от N¥»
6. В `#upsell` — подсказка «от N¥ выгоднее — X ₽» (если есть следующая ступень)
7. Кнопка «Оформить заявку» формирует deeplink в Telegram с подставленной суммой:
   `https://t.me/mvkhid?text=Здравствуйте! Нужна оплата 11750₽ → 1012,93¥ по курсу 11,60`

Форматирование чисел — `Intl.NumberFormat('ru-RU')`, 2 знака после запятой.

**При переносе:** ставки `tiers` должны приходить из `RATE_KV`, а не быть захардкожены. Захардкоженные значения в макете — фолбэк/пример.

---

## 6. Автокурс — главная фича, требует реализации

**Это ядро проекта.** Сейчас в макете курс вшит статикой. Нужно поднять цепочку:

```
Telegram @reseller_rmb → Cloudflare Cron (каждые 15 мин) → парсинг поста → RATE_KV → GET /rate → сайт (Vercel)
```

### Что нужно реализовать

1. **Cron Trigger** в `wrangler.toml` (Cloudflare Worker) — каждые 15 минут
2. **Парсер** — забирает последний пост с курсом из канала. Варианты источника:
   - публичный HTML `https://t.me/s/reseller_rmb` (без бота, но формат хрупкий)
   - Telegram Bot API (бот должен быть админом канала)
3. **Извлечь три ставки:** от 1¥, от 1000¥, от 30000¥ + дату публикации
4. **Записать в `RATE_KV`** JSON вида:
   ```json
   {
     "tiers": [
       {"threshold": 1, "rate": 11.75},
       {"threshold": 1000, "rate": 11.60},
       {"threshold": 30000, "rate": 11.55}
     ],
     "publishedAt": "2026-09-13T10:00:00+03:00",
     "updatedAt": "2026-09-13T10:15:00+03:00"
   }
   ```
5. **Worker отдаёт `GET /rate`** (JSON из KV, публичный, CORS открыт для домена сайта) — Next.js-сайт на Vercel запрашивает его при рендере и отдаёт в калькулятор + в подписи «Курс безналичного обмена · 13.09» и «обновлено · 13 сентября в 10:00»

### ⚠️ Обязательная подстраховка

Если формат поста в канале изменился или парсинг упал — **не показывать неверный курс**. Держать последний корректный из KV и логировать ошибку. Курс на сайте, не совпадающий с реальным, хуже чем слегка устаревший.

### ❗ Известная проблема
На прошлом live-адресе автокурс сломан — показывает майский курс. Причина не выяснена (cron не запускается / изменился формат поста / не пишет в KV / сайт не читает). При сборке заново проверить всю цепочку и залогировать каждый шаг.

**У владельца нужно запросить:** пример реального поста с курсом из @reseller_rmb, чтобы написать парсер под точный формат.

---

## 7. Ассеты — вынести из base64 в файлы

Сейчас всё зашито в HTML base64, файл весит **465 КБ**. Вынести:

| Что | Куда | Размер |
|---|---|---|
| 7 чеков операций | `/public/proof/check-1.jpg` … `check-7.jpg` | 400×400 каждый |
| Фото владельца (блок «Обо мне») | `/public/about.jpg` | 800×800 |

Извлечь можно скриптом из `preview-opt.html` — все картинки лежат как `data:image/jpeg;base64,...`.

**Чеки — важные нюансы отображения** (долго подбирали, не сломать):
- Контейнер `.proof-shot`: `aspect-ratio:7/8`, `object-fit:contain`, картинка прижата вверх (`align-items:flex-start`) — снизу остаётся небольшая белая полоса, это намеренно
- Фон карточки под скрином: `#f2f2f2`
- Клик → lightbox, там картинка целиком (`object-fit:contain`), без обрезки
- Порядок и подписи чеков (сумма · дата) — взять из макета как есть

**Фото** в блоке «Обо мне»: квадрат, `border-radius:24px`, тень, тонкая рамка.

---

## 8. Юридическая обвязка (уже написана, перенести дословно)

В макете готовы и **не требуют переписывания**:

1. **Публичная оферта** — модалка, 9 разделов (предмет, порядок услуг, акцепт, расчёты, права и обязанности с подпунктами по браку/отмене/страхованию/хранению, ответственность, форс-мажор, срок действия, реквизиты). Это реальный документ владельца, переносить дословно.
2. **Политика конфиденциальности** — модалка, по 152-ФЗ, с реквизитами ИП и каналами обращения.
3. **Cookie-баннер** — две кнопки «Только необходимые» / «Принять» (на мобайле первая сокращается до «Необходимые»), ссылка на политику. Без упоминания Яндекс.Метрики в тексте.
4. **Футер** — слева реквизиты ИП, справа: Политика · Публичная оферта · e-mail. Ссылки приглушённые (`--text-muted`), бирюзовые на hover.

Модалки: закрытие по клику вне, по `×`, по Escape. При открытии `body` блокируется от скролла.

**Даты в документах:** стоит 13 сентября 2026. При реальной публикации обновить на фактическую.

---

## 9. Что осталось сделать (чеклист)

### Обязательно для запуска — ВСЁ ГОТОВО, сайт в проде (2026-09-15)
- [x] Развернуть Next.js 15 проект (App Router) — задеплоен напрямую через Vercel CLI (не через git, см. «Известные хвосты» ниже)
- [x] Перенести весь дизайн из `preview-opt.html` в компоненты — см. «Как устроен перенос» ниже
- [x] Вынести ассеты (7 чеков + фото) в `/public/`
- [x] Поднять отдельный Cloudflare Worker: `RATE_KV` + Cron Trigger + `GET /rate` — `https://reseller-rate-worker.mgmdgdjiev1.workers.dev/rate`
- [x] Реализовать парсер поста из канала — проверен на реальных постах, работает в проде
- [x] Подключить калькулятор к `/rate` вместо хардкода
- [x] Обновить `ALLOWED_ORIGIN` в `worker-rate/wrangler.toml` на домен сайта
- [x] Задеплоить сайт на Vercel, проверить на мобильных

**Прод:** `https://site-iota-ruddy-38.vercel.app`

### Известные хвосты (не блокируют работу сайта, но стоит закрыть)
- [x] **Git-репозиторий подключён.** Новый репозиторий `mvkhid/china-site` на GitHub (старый `mvkhid/-1` не трогали — там прошлая версия). Vercel-проект `site` привязан к нему через GitHub App (пришлось доустановить сам Vercel App на GitHub — его не было). Теперь `git push` в `main` = автодеплой на Vercel.
- [ ] Перенос дизайна сделан прагматично под дедлайн: вся разметка/CSS/JS из `preview-opt.html` перенесены как raw HTML + vanilla JS (не переписаны на React-компоненты) — см. пояснение в разделе ниже. Работает полностью корректно, но при желании можно позже разбить на компоненты.

### Требует данных от владельца
- [ ] **Яндекс.Метрика** — нужен counter ID, потом в env как `NEXT_PUBLIC_YM_ID`
- [ ] **Домен** — планировался через reg.ru, уточнить куплен ли; до этого живёт на `.vercel.app`

### Отложено (не блокирует запуск)
- [ ] Блок «Примеры работ» на главной — фото товаров/партий, скрины заказов с 1688/Taobao, отзывы по закупкам. Владелец соберёт позже. Заглушка удалена, место освободилось после блока «Почему мне доверяют».
- [ ] Показать оферту юристу (особенно п. 4.4 про курс валют)
- [ ] Удалить `/api/diag` если он есть в старом проекте

---

## 10. Тексты — не «улучшать»

Тексты специально вычищены от ИИ-штампов и написаны голосом владельца. **Не переписывать, не «причёсывать», не добавлять от себя** блоков вроде «Всё легально и прозрачно», «за N секунд», «коротко о главном».

Что было убрано намеренно и не должно вернуться:
- любые упоминания **USDT**
- «Посчитайте сумму за 5 секунд», «Юани для разных задач», «Коротко о главном», «Чтобы вы были спокойны»
- канцелярит «обрабатываю по возможности»

Блоки сознательно сделаны **разноформатными** (чипы / кружки с цифрами / вертикальная линия слева / аккордеон / крупные цифры) — чтобы сайт не читался как ИИ-шаблон. Не унифицировать в одинаковые карточки.

**Мелочь, но важно:** при добавлении любых новых картинок — переименовывать файлы осмысленно (`icon-telegram.png`, а не `ChatGPT_Image_22.png`). Чистый код = солиднее.

---

## 11. Мобильная версия — вылизана, не сломать

Много точечных правок под мобайл, все в медиазапросах макета. Ключевое:

- Шрифты подогнаны по всем страницам (hero-заголовок `clamp(2.2rem,9vw,2.8rem)`, лиды `.95rem`, подписи `.82-.88rem`)
- Тарифные ступени калькулятора — вертикальным списком (слева «от 1¥», справа ставка)
- Нижний навбар `.demo-switch` — уменьшенные padding/шрифт, `white-space:nowrap` жёстко, чтобы «Как работает» не переносилось
- Cookie-баннер — кнопки в ряд, компактный
- Мини-статы — 3 колонки, текст по центру
- «Где закупаю» — 2 колонки (1 колонка на `<400px`)
- Футер — в столбик по левому краю
- Blob-эффект на touch **отключён**

---

## 12. Файлы в комплекте

- `preview-opt.html` — финальный макет, источник истины (дизайн + тексты + вшитые ассеты)
- `HANDOFF.md` — этот документ

Если понадобятся альтернативные варианты позиционирования — у владельца есть ещё два макета (`preview-full.html` — обменник в лоб, `preview-china.html` — оплата Китая). В работу идёт **preview-opt**.
